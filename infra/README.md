# Infrastructure

Local development infrastructure lives in this folder.

Use the provided Docker Compose file to start PostgreSQL and Redis for local work:

```bash
docker compose -f infra/docker-compose.yml up -d
```
